# First Class Exotics — handoff (11 Aug 2026, end of day)

Paste this whole file into a new Claude session to restore context.
Supersedes `HANDOFF-2026-08-10.md`. Everything below was verified by direct inspection or
against production — not carried from notes.

---

## 0. Current state

**Last commit `81d6c7b`.** Working tree clean apart from untracked docs (§6).

| Check | Result |
|---|---|
| Homepage image weight | **11.6 MB** (was 20 MB on 9 Aug) |
| GA4 conversion tracking | live sitewide, **0 public pages missing it** |
| GA4 key events | **done** — `generate_lead` + `contact_click` marked, receiving data |
| Fleet | **47 cars** (Porsche 911 Carrera 4 GTS added today) |
| Blog posts | 12, all with tracking |
| Broken links / images | 0 |
| Regression test | `npm run test:analytics` → 20/20 |

---

## 1. What shipped today

| Commit | Change |
|---|---|
| `2583a551` | Ace: added Porsche 911 Carrera 4 GTS — gallery, fleet card, dropdown |
| `d1a5bf3` | Recompressed those 5 photos q93 → q82 (saved 643 KB) |
| `b20b1ed` | Corrected the card specs to Ali's real numbers |
| `91aa251`–`5476787` | Blog publisher: new post, images, card, sitemap |
| `81d6c7b` | **Fixed blog-publisher template to inject conversion tracking** |

### The Porsche

Slug `porsche-911-gts-satin-grey` (directory name is intentionally shorter than the display
name — allowed, dir names are descriptive). Card reads **911 Carrera 4 GTS · 600 HP · AWD
Flat-Six · GT3 Center Locks · Satin Grey Custom · Red Interior.**

Ali's description of the car, verbatim: *600 HP, 911 Carrera 4 GTS, full satin grey, red
interior, GT3 center lock wheels, 911 Turbo S wing.* **No 0–60 figure was ever supplied —
none is claimed on the site or in the blog post. Do not invent one.**

Verified live: hero is the exterior front three-quarter, dropdown `value` is byte-identical
to the card's `data-car`, and clicking "Reserve Now" preselects the car in the booking form.

### The blog post

`/blog/porsche-911-carrera-4-gts-satin-grey` — published through Ali's Blog Publisher tool
(paste-article mode), 5 real photos, 0 broken links, meta description exactly 155 chars.

**Root-cause fix worth knowing about:** the Blog Publisher builds each post from a template
literal inside `blog-publisher.html` (~line 534). That template injected the GA4 snippet but
**not** `/js/fce-analytics.js`, so every new post was missing conversion tracking. Fixed in
the template, so future posts inherit it, and patched into the Porsche post. Same shape as
the `/blog/blog/` bug from June — the tool kept reintroducing it until the template was fixed.

**If a future post is missing tracking, check that template first.**

---

## 2. GA4 — complete

Property `542892966`, account `399008666`, measurement ID `G-E655N33GPP`.

- `generate_lead` and `contact_click` are **marked as Key Events** and showing live stream data
- 6 event-scoped custom dimensions: `vehicle`, `lead_type`, `days_out`, `method`,
  `link_placement`, `link_text`
- The scheduled task `ga4-mark-key-events` fired but **did not complete the starring** —
  it was done by hand. Task has auto-disabled. Nothing outstanding.
- Three pre-existing key events (`close_convert_lead`, `purchase`, `qualify_lead`) have
  never fired. Left alone.

**Note:** 4 of the 7 homepage phone links report `link_placement: sticky_bar` because the
header nav is also `position:fixed`. Use `link_text` alongside it to separate them.

Meaningful conversion data should exist by roughly **18 Aug**.

---

## 3. SEO — the real picture, from Search Console

Property is the **URL-prefix** property `https://www.firstclassexotics.com/`, not a domain
property. `sc-domain:firstclassexotics.com` returns "you don't have access" — don't chase it.

**Last 3 months:** 980 clicks · 59,900 impressions · 1.6% CTR · **average position 30.2**

Position 30 is page 3. The demand is there; the ranking is not.

| Query | Impressions | Clicks |
|---|---|---|
| first class exotics | 359 | 106 |
| **first class exotics daytona beach fl** | 117 | **48** |
| first class exotics florida | 32 | 15 |
| **exotic car rental orange county** | **987** | **13** |
| luxury car rental orange county | 489 | 8 |
| exotic car rental | 386 | 10 |

**63 clicks go to people looking for a different company** — there is a "First Class
Exotics" in Daytona Beach, Florida. Those visitors will never book in Newport Beach.

### Indexing: 49 indexed / 64 not indexed, 8 reasons

| Reason | Pages | Validation |
|---|---|---|
| Alternate page with proper canonical tag | 9 | **Failed** |
| Page with redirect | 14 | Not started |
| Not found (404) | 3 | Not started |
| Excluded by noindex | 2 | Not started |
| Discovered – currently not indexed | 21 | Not started |
| Crawled – currently not indexed | 12 | Not started |
| Duplicate, Google chose different canonical | 3 | Not started |

### Probable cause of the 33 not-indexed pages — measured, not guessed

Word-overlap analysis of the page groups:

| Group | Pages | Avg word overlap |
|---|---|---|
| **Vinyl wrap pages** | 10 | **79%** |
| Car pages (`rent-*`) | 8 | 45% |
| Location pages (`exotic-car-rental-*`) | 7 | 39% |

The ten vinyl wrap pages are ~79% identical to each other (Huntington Beach vs San Clemente
is 80%). Google treats that as doorway pages and declines to index. **Rewriting these so
each is genuinely about its city is the most likely fix for the not-indexed count.**

---

## 4. Google Business Profile — this is where the leverage is

Ali manages the profile. **He is already #2 in the local map pack** for "exotic car rental
orange county" — an earlier assumption that he was missing from it was wrong.

| Business | Rating | Reviews | Position |
|---|---|---|---|
| Mango Exotics | 5.0 | **187** | #1 |
| **First Class Exotics** | 5.0 | **81** | **#2** |
| Luxe Horizon | 5.0 | 15 | #3 |
| Monza Exotics | 5.0 | **410** | buying ads |

Profile: 10,180 views, 1,296 customer interactions, **4 unread reviews**, Profile Strength
reported as good. Address 2060 Placentia Ave Ste A4, Costa Mesa, CA 92627.

**Review count is the gap between #2 and #1.** Rating is already perfect.

**Review link:** `https://g.page/r/CQ_Rg94B1MthEBM/review` — verified working, opens the
review box directly.

Full playbook with message templates is in **`REVIEW-SYSTEM.md`** (untracked, repo root).

### ⚠️ Two warnings

**Do NOT start the Google Workspace trial** that Google's profile flow pushes. It walks you
through repointing MX records at Google. **Ali's email runs through Wix**, and the hard rule
is never touch Wix DNS. Following that flow breaks `ali@firstclassexotics.com`.

**Google's "Complete Info" / "Customize profile" flow is three upsell screens** — $500 Ads
credit, then Workspace, then done. It is not a completeness checklist. Google's own fine
print says "You can fully complete your profile without advertising with Google Ads."

---

## 5. Still open

### Needs Ali, not an agent

- **FAQ insurance wording.** Live text claims personal auto insurance "typically extends to
  rental vehicles, including exotic rentals." Contradicts Laura; liability exposure on $300k
  cars. Ali is verifying with his insurer. **Do NOT let an agent rewrite this into different
  confident language** — that swaps one unreviewed legal claim for another.
- **Review backlog push.** The fastest path from 81 → 187. Needs Ali's past-customer list
  (there is a `Client List FC` folder and a CRM folder on his Desktop, not yet examined).
- **6 orphan fleet directories** (~13.6 MB of raw originals). Safe to delete but may be the
  only copies. Confirm first.
- **"46+" copy vs 47 actual cars.** Canonical copy in `CLAUDE.md` says 46+. Adding the
  Porsche makes 47. Ali's call whether the marketing number moves.
- **`blog/exotic-cars-coming-orange-county-2026-2027.html`** — no phone link, no CTA, no nav.
  Traffic landing there cannot convert.

### Site work, in priority order

| Item | Detail |
|---|---|
| **Vinyl wrap page rewrite** | 79% duplicate content, 10 pages. Best lever on the not-indexed count. |
| **Search Console cleanup** | Fix the 3 404s; re-run the failed canonical validation; investigate the 21 "Discovered – not indexed". |
| **WebP conversion** | 11.6 MB → ~8 MB. Needs `<picture>` markup on ~350 images across 53 files. **Do not cap dimensions below 1600px — the lightbox needs them.** |
| **Meta lengths** | 35 of 50 titles over 60 chars, 23 of 50 descriptions over 160. At 60K impressions, CTR gains compound. |
| Minor | `/privacy` and `/terms` lack JSON-LD and `og:image`. Confirm `blog-publisher.html`'s password gate validates server-side, not just client-side. |

---

## 6. Untracked files in the repo — Ali's call

```
.vscode/                                  HANDOFF-2026-07-30.md
ACE-PROMPT-porsche-911-gts.md             STATUS-2026-07-30.md
BLOG-PUBLISHER-porsche-911-carrera-4-gts.md   VOICE-AGENT-PLAN.md
REVIEW-SYSTEM.md
```

`REVIEW-SYSTEM.md` is worth committing. `.vscode/` should probably be gitignored.
Undecided — do not commit them without asking.

---

## 7. Environment

- Repo `github.com/ahojat45/firstclassexotics`, branch `main`
- Local path `/Users/firstclassexotics/Desktop/First Class Exotics` — **mount it first thing**
- Netlify site_id `fbd7706b-a7f3-471d-a38e-9583a11dbcca`, auto-deploys on push to `main`
- 78 redirect rules in `netlify.toml`, first-match-wins, no `/*` catch-all
- Connectors live: Google Drive, Gmail, Calendar, Chrome. QuickBooks/PayPal/HubSpot/Square/
  Stripe/Slack/Canva/DocuSign appear but are **not authorized**.

### Hard rules

- **Never touch Wix DNS.** Wix is live only for email MX records.
- **Never send email on Ali's behalf. Drafts only, always.**
- Netlify primary domain stays `www.firstclassexotics.com`; apex 301s to `www`.
- More than one agent works in this repo — confirm the tree is yours before committing.
- Never redirect or block: `google49d716d460298fda.html`, `google8cf0ce3903a867e7.html`
  (Search Console verification), `404.html`, `blog-publisher.html`, `fce-os/`, `agreement.html`.

### Workflow

Claude edits files directly once the folder is mounted. **Ace is rarely needed** — he can't
reach Google Drive and his sandbox can't push. When Ace is used, give him direct Drive
download URLs (`https://drive.google.com/uc?export=download&id=FILE_ID`) and require a
`COMMITTED — not pushed` report.

Ali pushes from **Terminal.app** (never the VS Code terminal — sandboxed):

```
git add -u && git commit -m "..." && git push origin main
```

**Known trap:** a sandboxed agent running `git status` can leave a stale `.git/index.lock`
it lacks permission to delete. Ali then sees `fatal: Unable to create ... index.lock: File
exists`. No git process is really running. Fix: `rm -f .git/index.lock`.

### Site conventions

- Fleet images `images/fleet/<slug>/<slug>-NN.jpg`, relative, NN from `01`; dir and stem must match
- `-01` must be an exterior front three-quarter; interiors last
- `<img>` carries `data-gallery`, `width="800"`, `height="533"`, `loading="lazy"`
- Dropdown `<option>` value and label must be identical strings, and must match the card's `data-car`
- Fleet photos are 1600px q82 progressive — **keep new additions at q82**
- Blog URLs `/blog/<slug>`. Links from a blog post need a leading slash; relative
  `blog/<slug>` resolves to `/blog/blog/<slug>` and 404s
- Canonical copy: "12+ years", "since 2014"

### Scheduled tasks (all on the iMac, only run while the app is open)

| Task | Schedule |
|---|---|
| fce-lead-calendar-sync | every 2 hours |
| fce-sync-watchdog | 9am / 2pm / 7pm |
| fc-exotics-inbox-monitor | 8am / 6pm |
| bioaminex-inbox-monitor | 8am / 6pm |
| ga4-mark-key-events | done, auto-disabled |
| fce-os-redesign-check | disabled |

Definitions live in `/Users/firstclassexotics/Claude/Scheduled/<name>/SKILL.md`, **not in
this repo.**

**Unresolved anomaly:** on 11 Aug `fc-exotics-inbox-monitor` **skipped its 8am run**
(last run 6:03pm 10 Aug, next 6:03pm 11 Aug) while `bioaminex-inbox-monitor` ran normally at
8:07am on the same machine and schedule. Not investigated. Given the July incident where a
dead sync left 20 booking-form leads unanswered for 6–9 days, **this is worth checking.**

---

## 8. How Ali works

- Concise and direct. Short answers, no padding. He gets frustrated with process overhead
  and clarifying questions when he's asked for something specific — bias toward doing.
- **Verify against production. Never say something is fixed without checking.** He is right
  to ask for proof.
- He is not a developer. Give exact copy-paste commands and say where to paste them
  (**Terminal**, never Ace, never the VS Code terminal).
- **Distinguish broken from optimization every time.** An undifferentiated fix list reads as
  "my site is full of problems" when it isn't. He has pushed back on this directly.
- **Show, don't assert, on anything visual.** He cares a great deal how the cars look and
  that judgement is his.
- Own mistakes plainly and move on. When a hypothesis turns out wrong (e.g. assuming the
  Google Business Profile was missing when he was already #2), say so directly.
