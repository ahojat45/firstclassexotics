# First Class Exotics — handoff (13 Aug 2026, end of day)

Paste this whole file into a new Claude session to restore context.
Supersedes `HANDOFF-2026-08-11.md`. Everything below was verified against production
or by direct inspection — not carried from notes.

---

## 0. Current state

**Last commit `511acc9`.** Working tree clean apart from the untracked docs (§7).
Local `main` == `origin/main`. Everything below is live.

| Check | Result |
|---|---|
| Fleet | **48 cars** (2022 McLaren 720S MSO Spider — Satin Black added today) |
| Homepage image weight | **8.1 MB** (was 12.7 MB this morning, 20 MB on 9 Aug) |
| Gallery (lightbox) weight | **37.7 MB** (was 61.2 MB) — 268 images, all WebP |
| Images referenced with no `.webp` sibling | **0** (was 249) |
| Images not wrapped in `<picture>` | **0** (was 2) |
| Titles over 60 chars | **0 of 52** (was 35) |
| Descriptions over 160 chars | **0 of 52** (was 23) |
| Internal links hitting a 301 | **0** (was **1,029**) |
| Orphan pages | **0** (was 5) |
| Sitewide JSON-LD errors | **0** (was 8) |
| Blog posts listed on /blog | **19 of 19** (was 14) |
| Sitemap | 51 entries, 0 dupes, 0 `.html` |
| Broken images / links | 0 |
| Google reviews | **84** (schema + visible text both updated) |

---

## 1. What shipped today — 9 commits

| Commit | Change |
|---|---|
| `7ff9dfd` | Redirect `/shop` and `/copy-of-bmw-m3-competition-g80` (Search Console 404s) |
| `8eae91f` | Rewrote 10 vinyl-wrap city pages; duplication 67% → 39%; FAQPage schema added |
| `ec9b63c` | All titles ≤60 and descriptions ≤160; review count 80 → 84 |
| `1da8634` | **WebP + `<picture>` sitewide**; homepage 12.7 MB → 7.9 MB |
| `7c08016` | Fixed JSON-LD on 8 blog posts (JS was inside the schema block) |
| `34eb0ac` | Ace: added 2022 McLaren 720S MSO Spider — Satin Black |
| `ce982ab` | **Rewrote all 1,029 internal links to served URLs**; /blog lists every post; linked orphaned Ferrari F8 |
| `44caaf1` | Removed a duplicate card + sitemap entry I added in error (§8) |
| `511acc9` | **Gallery WebP**: generated 249 missing `.webp`; lightbox now serves WebP with JPEG fallback; wrapped the 2 remaining bare `<img>` |

---

## 2. ⚠️ TWO CONVENTIONS CHANGED TODAY — read before editing anything

### Every image needs a `.webp` sibling and a `<picture>` wrapper

```html
<picture><source srcset="path/name-01.webp" type="image/webp"><img width="800" height="533" loading="lazy" src="path/name-01.jpg" alt="..." data-gallery="..." onerror="this.style.background='#1C1C1C'"></picture>
```

WebP at **quality 82, identical pixel dimensions** to the JPEG. Never upscale — if the
source is smaller than 1600px, leave it (see §6). **365 `.webp` files exist now — every
JPEG referenced anywhere on the site has one, including gallery-only images.**

The rule now covers `data-gallery` too, not just `src`. The homepage lightbox calls
`webpUrl()` and requests the `.webp` first, falling back to the JPEG on error — so a new
car added without `.webp` files will still display, just heavier. Adding them is not
optional; the fallback exists to prevent a broken gallery, not to excuse skipping the step.

### Internal links must point at the SERVED URL, never `.html`

`href="/gift"` not `href="gift.html"`. `href="/blog/<slug>"` not `href="blog-<slug>.html"`.
From a `/blog/` page, `href="/"` not `href="../index.html"`.

This is the same rule that already applied to canonical, og:url and sitemap — it now
applies to `href` too. **1,029 links violated it until today.** If a generator or the
Blog Publisher reintroduces `.html` hrefs, that is a regression.

Never rewrite these five: `404.html`, `blog-publisher.html`, `agreement.html`,
`google49d716d460298fda.html`, `google8cf0ce3903a867e7.html`.

---

## 3. 🔴 UNFINISHED — Request Indexing queue, resume 14 Aug

Google caps Request Indexing at roughly 10–12 URLs per property per day. We hit
**Quota Exceeded** on 13 Aug at 2:37 PM.

### Already requested (do NOT resubmit — resubmitting does not improve priority)

11 Aug / earlier today: `/rent-maybach-orange-county`, `/rent-g63-orange-county`,
`/rent-lamborghini-orange-county`, `/blog/porsche-911-gt3-rs-40`,
`/blog/oc-exotic-car-culture-guide`
13 Aug: `/` (confirmed), `/exotic-car-rental-newport-beach` (confirmed),
`/exotic-car-rental-costa-mesa` (clicked, confirmation not captured — treat as done)
**14 Aug — 11 submitted, all confirmed by Ali:** `/exotic-car-rental-irvine`,
`/exotic-car-rental-laguna-beach`, `/exotic-car-rental-huntington-beach`, `/vinyl-wrap`,
`/vinyl-wrap-newport-beach`, `/vinyl-wrap-costa-mesa`, `/car-ferrari-f8`,
`/vinyl-wrap-irvine`, `/vinyl-wrap-laguna-beach`, `/vinyl-wrap-huntington-beach`,
`/vinyl-wrap-corona-del-mar`.
**Quota Exceeded hit on the 12th** (`/vinyl-wrap-dana-point`) at 10:56 AM — it did NOT
go through. Observed cap is **11 per day**, both on 13 and 14 Aug.

All 15 URLs below were re-checked against production on 13 Aug after the `511acc9` deploy:
every one returns **200, no redirect, correct self-canonical, no `noindex`**. Safe to submit.

### ✅ 14 Aug — 11 submitted. Do not resubmit any of them.

### ▶ START HERE on 15 Aug — only 4 left, then the queue is empty

```
https://www.firstclassexotics.com/vinyl-wrap-dana-point
https://www.firstclassexotics.com/vinyl-wrap-mission-viejo
https://www.firstclassexotics.com/vinyl-wrap-san-clemente
https://www.firstclassexotics.com/vinyl-wrap-yorba-linda
```

After these four, **every page worth submitting has been submitted.** Do not invent more
Request Indexing work — from here it is a waiting game on the internal-link fix (§2).

### How to do it (automation works, with one quirk)

Search Console → click the inspect bar at the very top → paste URL → Enter → wait
~10s → click **REQUEST INDEXING** → wait for the green "Indexing requested" → Dismiss.

Driving it by coordinates is unreliable. What works is clicking the search bar at
roughly (770, 33) on a 1501px-wide window, typing, pressing Return, then clicking the
button **via JS** rather than by coordinate:

```js
[...document.querySelectorAll('*')]
  .filter(e=>e.childElementCount===0 && /^REQUEST INDEXING$/i.test((e.textContent||'').trim()))[0].click()
```

**Keep this in proportion.** Request Indexing is a nudge. The durable fix is the
internal-link rewrite in §1 — that is what gets the other ~40 pages crawled without
asking one at a time.

---

## 4. Search Console — the real picture (pulled 13 Aug, not from notes)

Property is the **URL-prefix** property `https://www.firstclassexotics.com/`.
49 indexed / 64 not indexed. Last 3 months: 980 clicks · 59,900 impressions ·
1.6% CTR · **average position 30.2**.

**"Discovered – currently not indexed" (21).** Every one shows **Last crawled: N/A** —
Google found the URL and never fetched it. Not a content problem. Cause was crawl
budget burned on 1,029 redirect hops + orphaned pages. Both fixed today.
List: 9 blog posts, `/car-ferrari-f8`, `/terms`, and all 10 vinyl-wrap city pages.

**"Crawled – currently not indexed" (12).** 11 of 12 are **dead Wix ghost URLs with no
file on disk** (`/copy-of-blue-r8`, `/lambo-huracan-red`, `/post/roger-dubuis`, …), last
crawled Feb–May 2026, before the July redirect cleanup. **Stale data. Nothing to fix.**
Do not chase these.

**"Alternate page with proper canonical tag" (9), validation Failed.** Also stale —
Google's stored canonical for these predates the 25 July fix. Verified the live files
all declare correct extensionless self-canonicals. Requesting indexing is the fix, not
re-running the validation.

**404 validation** started 12 Aug on all 3 URLs, still running.

**Stale-data trap seen again on 14 Aug.** The inspection panel for `/vinyl-wrap-dana-point`
showed "No referring sitemaps detected" and a referring page of
`…/vinyl-wrap-mission-viejo.html`. Both were checked against the repo and are **stale Google
data, not defects**: all 10 vinyl-wrap cities are in `sitemap.xml` (51 entries, 0 dupes,
0 `.html`, 0 pages on disk missing from it), and a full grep found **zero** `.html` hrefs
outside the five protected files. Google simply has not fetched the page yet
(Last crawl: N/A). Do not "fix" either signal.

**63 of 980 clicks go to a different company** — there is a "First Class Exotics" in
Daytona Beach FL. Unwinnable. Exclude it when measuring anything.

---

## 5. Google Business Profile

**#2 in the local map pack** for "exotic car rental orange county". 5.0 rating.
**84 reviews** as of 13 Aug (was 81 on 11 Aug — Ali is actively working this).
Mango Exotics is #1 with 187. **Review count is the entire gap.**

Review link: `https://g.page/r/CQ_Rg94B1MthEBM/review` — verified working.
Playbook in `REVIEW-SYSTEM.md` (untracked, repo root).

**Do NOT start the Google Workspace trial** — it repoints MX records and Ali's email
runs through Wix. Google's "Complete Info" flow is three upsell screens, not a checklist.

---

## 6. Still open

### Needs Ali, not an agent

- **FAQ insurance wording.** Live text claims personal auto insurance "typically extends
  to rental vehicles, including exotic rentals." Contradicts Laura; liability exposure on
  $300k cars. Ali is verifying with his insurer. **Do NOT let an agent rewrite this into
  different confident language.**
- **Review push, 84 → 187.** Biggest business lever on the board. Needs Ali's past-customer
  list — there is a `Client List FC` folder and a CRM folder on his Desktop, not yet opened.
- **"46+" copy vs 48 actual cars.** In `CLAUDE.md` and on the homepage. Ali's call.
- **New McLaren photos are 1177px**, vs 1600px for the rest of the fleet. Source files in
  Drive are natively that size, so Ace correctly did not upscale. If bigger originals
  exist, swap them in.
- **6 orphan fleet directories** (~13.6 MB of raw originals). Safe to delete, may be the
  only copies. Confirm first.

### Site work

| Item | Detail |
|---|---|
| `/privacy`, `/terms` | No JSON-LD, no `og:image`. ~15 minutes. |
| Location + car pages | Measured: only **9%** and **21%** cross-page duplication. The handoff's old "39% / 45%" figures used a looser metric and overstated it. **These do not need rewriting.** |

---

## 7. Untracked files — Ali's call

```
.vscode/                                    HANDOFF-2026-07-30.md
ACE-PROMPT-porsche-911-gts.md               STATUS-2026-07-30.md
ACE-PROMPT-mclaren-720s-mso-satin-black.md  VOICE-AGENT-PLAN.md
BLOG-PUBLISHER-porsche-911-carrera-4-gts.md REVIEW-SYSTEM.md
```

`REVIEW-SYSTEM.md` is worth committing. `.vscode/` should be gitignored.
Do not commit any of these without asking.

---

## 8. Mistakes made on 13 Aug — do not repeat

Three misreads, all the same root cause: **judging a page by its filename instead of its
served URL.**

1. Called the 8 root-level `blog-*.html` files "dead" and recommended deleting them.
   They are **live posts** served at `/blog/<slug>` via **status-200 rewrites** at
   `netlify.toml` line ~146. Deleting them would have removed 8 posts, 3 of them linked
   from the homepage.
2. Said those 8 were missing from the sitemap. They are in it, under their pretty URLs.
   The grep searched for the filename.
3. Added `/blog/lamborghini-temerario-the-huracan-era-is-ending` to the blog index and
   sitemap as a "missing post". It is a **retired duplicate** that 301s to
   `/blog/lamborghini-temerario-rental-california`. Reverted in `44caaf1`.

**Rule: before calling any page dead or missing, follow the redirect chain to the end and
check what the URL actually returns.** `netlify.toml` has 80 rules, including 200-rewrites
that make root files serve at nested URLs.

---

## 8b. Verified on production after `511acc9`

Checked live, not locally: all 48 fleet cards serve `.webp`; the SF90 gallery steps through
all 7 slides as `.webp` at full resolution; a HEAD check of all **268 gallery `.webp` URLs**
returned zero missing; `/rent-ferrari-orange-county` serves `images/ferrari/05.webp`.
Fallback path was tested by deleting a `.webp` — it served the JPEG with no console error.

**Note for whoever picks this up:** the 13 Aug WebP commit (`1da8634`) only converted the
123 card thumbnails. The 249 gallery images were missed and were still serving as JPEG until
`511acc9`. If a similar "sitewide" image change lands in future, check `data-gallery` targets
explicitly — they are not reachable by grepping `src=`.

---

## 8c. Blog Publisher security fix — 14 Aug (commit `6bb5b0a`)

The §6 item "confirm the password gate validates server-side" was checked on 14 Aug.
**It did not.** Both halves are now resolved:

**Was broken.** `blog-publisher.html` shipped `const PASSWORD='FCE2026'` to every visitor,
and `netlify/functions/blog-publisher.js` compared against the same hardcoded literal.
The page is public (200, no auth), so anyone who viewed source had a working credential to
publish to the live site through the function. Abuse check found none — every commit in the
prior 60 days was one of Ali's three git identities, every post legitimate. `GITHUB_TOKEN`
and `ANTHROPIC_API_KEY` were always env vars and were never exposed.

**Now.** Password lives only in Netlify env var **`BLOG_PUBLISHER_PASSWORD`**
(Production scope, marked secret, scopes include Functions). Server **fails closed** with
500 if it is unset — there is no fallback and none should ever be added. Compare is
`crypto.timingSafeEqual`. The client holds nothing: the operator types the password, it sits
in `sessionStorage` for that tab only, login verifies via `operation:'auth'`, and logout
clears it. Page also carries `noindex`.

Verified on production 14 Aug: wrong password → 401, empty → 401, **old `FCE2026` → 401
(dead)**, no secret in served source, and Ali logged in successfully with the new password.

⚠️ **If the publisher ever returns 500 "BLOG_PUBLISHER_PASSWORD not configured", the env var
was deleted or the scope changed. Re-add it in Netlify — do NOT reintroduce a hardcoded
default.** The old password is in git history forever and must never be reused.

**Extensionless hrefs (the other half of the §6 item): CONFIRMED GOOD.** The function strips
`.html` from canonical and `og:url`, and rewrites blog-card hrefs to the extensionless form,
before committing. `blog/<slug>.html` appears only as the on-disk file path, which is correct
— those serve at `/blog/<slug>` via the netlify.toml 200-rewrites (§8).

---

## 9. Environment

- Repo `github.com/ahojat45/firstclassexotics`, branch `main`
- Local path `/Users/firstclassexotics/Desktop/First Class Exotics` — **mount it first thing**
- Netlify site_id `fbd7706b-a7f3-471d-a38e-9583a11dbcca`, auto-deploys on push to `main`
- **80 redirect rules** in `netlify.toml`, first-match-wins, no `/*` catch-all.
  Includes 200-rewrites mapping `/blog/<slug>` → `blog-<slug>.html` — see §8.
- GA4 property `542892966`, measurement ID `G-E655N33GPP`. `generate_lead` and
  `contact_click` are Key Events and receiving data. Meaningful volume by ~18 Aug.
- Connectors live: Google Drive, Gmail, Calendar, Chrome, Netlify.
  QuickBooks/PayPal/HubSpot/Square/Stripe/Slack/Canva/DocuSign appear but are **not authorized**.

### Hard rules

- **Never touch Wix DNS.** Wix is live only for email MX records.
- **Never send email on Ali's behalf. Drafts only, always.**
- Netlify primary domain stays `www.firstclassexotics.com`; apex 301s to `www`.
- Never redirect or block `google49d716d460298fda.html`, `google8cf0ce3903a867e7.html`,
  `404.html`, `blog-publisher.html`, `fce-os/`, `agreement.html`.
- Only mark up schema for content a visitor can actually see on that page.
- `reviewCount` in schema and the visible "N Google reviews" text must change together.
- More than one agent works in this repo — confirm the tree is yours before committing.

### Workflow

Claude edits files directly once the folder is mounted. **Claude cannot push** (no network
on the device bridge). Ali pushes from **Terminal.app** (never the VS Code terminal):

```
cd '/Users/firstclassexotics/Desktop/First Class Exotics' && rm -f .git/index.lock && git add -u && git commit -m "..." && git push origin main
```

Use `git add -A` instead of `-u` when new files were created (e.g. new images).

**Known trap, hit repeatedly:** any `git status` / `git diff` run from the sandbox leaves a
stale `.git/index.lock` it cannot delete. Ali then sees `fatal: Unable to create ...
index.lock: File exists`. Always put `rm -f .git/index.lock` in the push command.

**Ace** is rarely needed — he cannot reach Google Drive and cannot push. When used, give
him direct Drive download URLs (`https://drive.google.com/uc?export=download&id=FILE_ID`),
spell out the §2 conventions (he will not know them), and require a
`COMMITTED — not pushed` report. `ACE-PROMPT-mclaren-720s-mso-satin-black.md` is a good
template — that task landed correctly first time.

### Site conventions

- Fleet images `images/fleet/<slug>/<slug>-NN.jpg` **+ matching `.webp`**, NN from `01`
- `-01` must be an exterior front three-quarter; interiors last
- Fleet photos 1600px q82 progressive where the source allows — **never upscale**
- Dropdown `<option>` value, its visible label, and the card's `data-car` must be the
  identical string. The car `<select>` lives in **`index.html` only** — the `name="vehicle"`
  on vinyl-wrap pages is a text input.
- Blog URLs `/blog/<slug>`. Canonical copy: "12+ years", "since 2014".

### Scheduled tasks (all on the iMac, only run while the app is open)

| Task | Schedule |
|---|---|
| fce-lead-calendar-sync | every 2 hours |
| fce-sync-watchdog | 9am / 2pm / 7pm |
| fc-exotics-inbox-monitor | 8am / 6pm |
| bioaminex-inbox-monitor | 8am / 6pm |

Definitions in `/Users/firstclassexotics/Claude/Scheduled/<name>/SKILL.md`, not in this repo.
Lead sync verified healthy on 13 Aug — recent booking-form leads all carry the
`FCE-Calendar-Synced` label. **If Ali works from the MacBook, these stop running.**

---

## 10. How Ali works

- Concise and direct. Short answers, no padding. He gets frustrated with process overhead
  and clarifying questions when he's asked for something specific — bias toward doing.
- **Verify against production. Never say something is fixed without checking.**
- He is not a developer. Give exact copy-paste commands and say where to paste them
  (**Terminal**, never Ace, never the VS Code terminal).
- **Distinguish broken from optimization every time.**
- **Show, don't assert, on anything visual.** Screenshots beat descriptions.
- He often misremembers which of two similar tasks was completed. Check before agreeing
  or disagreeing — twice today he was half right, and the specifics mattered.
- Own mistakes plainly and move on.
