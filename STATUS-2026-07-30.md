# Status Check — First Class Exotics

**Run:** Thursday, July 30, 2026, ~9:45 AM PDT
**Scope:** live site, FCE OS, Supabase, scheduled tasks, leads

---

## Bottom line

**Everything is up and running.** Site is live, FCE OS is live, Supabase is on Pro, the scheduler bug is fixed and holding.

**One thing needs you: a `git push`.** Details at the bottom. Until you run it, the FCE OS search-indexing fix sits committed locally but isn't deployed.

---

## Live site — healthy

- Netlify deploy state: **`ready`** / **`current`** (deploy `6a67bcde67460c0008883887`)
- Homepage fetched from production and renders completely — all 44 fleet cards, images, booking form, blog links
- Primary domain resolving correctly on `www.firstclassexotics.com`
- Netlify Forms: **enabled**

### Repo integrity re-verified today

| Check | Result |
|---|---|
| Fleet dirs referenced by `index.html` | 44 |
| Referenced but missing on disk | **0** |
| Filename convention violations | **0** |
| PNGs under `images/` | **0** |
| Images over 800 KB under `images/` | **0** |

Unchanged since the July 27 audit. Nothing regressed.

---

## FCE OS — up

Fetched `https://www.firstclassexotics.com/fce-os/` from production. The login shell renders, the password gate is in place, and the full dashboard app shell (Overview / Pipeline / Customers / Documents) is present.

The `fce-os-agreements-get-public` function endpoint responds, confirming the Netlify functions are deployed.

**Caveat on what I verified:** I confirmed the infrastructure — the page loads, functions are deployed, Supabase reports healthy. I did **not** log into the dashboard and read a live record end-to-end, so I can't personally confirm data is flowing through to the UI. If you want that certainty, log in and check that the Pipeline tab shows your 27 rows.

---

## Supabase — now on Pro

Per your upgrade screen, **FC Exotics Org is on Pro at $25/month.** That resolves both risks:

- **No more auto-pausing.** The inactivity warning that started this is now moot.
- **Daily backups, 7-day retention.** Previously you had **no backups at all** on a database holding customer driver's licenses, insurance cards, and signed rental agreements. This was the more serious of the two gaps.

The first backup runs on Supabase's schedule — give it 24 hours before "LAST BACKUP" populates on the project page. Worth a glance tomorrow to confirm it did.

Because pausing is off the table, the keep-alive workaround is no longer needed and was not built.

---

## Scheduled tasks — fix is holding

`fce-lead-calendar-sync` has run roughly 24 times since the fix went in. **Zero new duplicates.**

Three straggler events were created by a single run that was already in flight during the fix (all timestamped within a minute of the change). All three deleted:

- Derrick Disbrow (deleted July 28)
- Diva Espinoza (deleted today)
- Adam Sheble (deleted today)

Calendar is back to **exactly one APPROVAL NEEDED event per lead.**

All three tasks enabled and firing on schedule:

| Task | Schedule | Last run |
|---|---|---|
| `fce-lead-calendar-sync` | every 2 hours | Jul 29, 4:09 PM PDT |
| `fc-exotics-inbox-monitor` | 8am & 6pm daily | Jul 29, 8:03 AM PDT |
| `bioaminex-inbox-monitor` | 8am & 6pm daily | Jul 29, 8:09 AM PDT |

---

## What I changed today

Committed as `afef9c0` — **not yet pushed.**

1. **Blocked FCE OS from search indexing.** Added `noindex,nofollow,noarchive,nosnippet` to `fce-os/index.html`, matching what `agreement.html` already had, and added `Disallow: /fce-os/` to `robots.txt`. Confirmed the exposure was real: fetching `/fce-os/` from production returned the page title and dashboard section headings, all crawlable despite the password gate.
2. **Added `CLAUDE.md`** — operating rules, fleet image convention, verified repo state, and an explicit note that the scheduled tasks are *not* in this repo, so future sessions don't waste time searching for them.
3. **Added `SITE-AUDIT-2026-07-27.md`.**

### Also fixed: stale git locks

Three lock files were left behind by the Ace session that crashed on July 27 (`index.lock`, `HEAD.lock`, `objects/maintenance.lock`). They were blocking commits. All removed; working tree is clean.

---

## Action needed from you

The commit is local. Netlify deploys on push, so the indexing fix isn't live until you run:

```bash
cd '/Users/firstclassexotics/Desktop/First Class Exotics'
git push origin main
```

I couldn't do it — the sandbox has no GitHub credentials, by design.

---

## Still open — bookings

Friday's two deliveries have gaps I flagged on the calendar events but can't resolve without you:

| When | Who | Missing |
|---|---|---|
| **Fri Jul 31, 1:30 PM** | Keith Oneal Davis | Which airport, arrivals vs Pelican Hill, and which F8 — Spider or Tributo |
| **Fri Jul 31, 11:00 AM** | Lincoln K | Return time on Aug 3 (assumed 11 AM) |
| **Fri Aug 7, 4:00 PM** | Tarunjeet Bajwa | Vehicle not specified |
| **Fri Aug 7, 2:20 PM** | Alvin Yono | Which airport JSX flies from |

**Derrick Disbrow's stated start date was yesterday, July 30.** His draft is still unsent and no car was locked as of our last conversation. This is the oldest unresolved item.

**Aug 7 is stacking up** — Alvin at 2:20 PM and Tarunjeet at 4:00 PM, roughly 40 minutes apart by road. If Ella Holm's Cullinan is a *separate* Ritz-Carlton wedding rather than the same one as Tarunjeet's, that's a third delivery the same afternoon.

**Nine drafts still unsent:** Derrick, Scott, Diva, Ella, Tien, Dylan, Karina, Jordan, Safi.
